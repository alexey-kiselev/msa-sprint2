replicaCount: 1
version: v1

image:
  name: booking-service
  tag: latest
  pullPolicy: Never

service:
  enabled: true
  type: ClusterIP
  port: 80
  targetPort: 8080

ENABLE_FEATURE_X: "false"

env:
  - name: APP_ENV
    value: prod
  - name: LOG_LEVEL
    value: info

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

livenessProbe:
  httpGet:
    path: /ping
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  httpGet:
    path: /ping
    port: http
  initialDelaySeconds: 3
  periodSeconds: 5
